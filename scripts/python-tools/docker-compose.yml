services:
  exporter:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: student-data-exporter
    ports:
      - "8000:8000"
    env_file:
      - .env
    restart: unless-stopped
